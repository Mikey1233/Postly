const supabase = require('./supabase');

module.exports = {
  async getByPlatform(platform) { throw new Error('db.platformGroups.getByPlatform not yet implemented'); },
  async upsert(platform, groups) { throw new Error('db.platformGroups.upsert not yet implemented'); },
};
